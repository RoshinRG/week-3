<?xml version="1.0" encoding="UTF-8"?>
<!-- ============================================================
     web.xml — Deployment Descriptor
     Place this file inside: WebContent/WEB-INF/web.xml
     Use this if your server does NOT support @WebServlet annotation
     (Servlet 3.0+). Otherwise, @WebServlet("/hello") is enough.
================================================================ -->
<web-app xmlns="http://java.sun.com/xml/ns/javaee"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://java.sun.com/xml/ns/javaee
             http://java.sun.com/xml/ns/javaee/web-app_3_0.xsd"
         version="3.0">

    <!-- Servlet declaration -->
    <servlet>
        <servlet-name>HelloWorldServlet</servlet-name>
        <servlet-class>HelloWorldServlet</servlet-class>
    </servlet>

    <!-- Servlet mapping: maps URL /hello to the servlet above -->
    <servlet-mapping>
        <servlet-name>HelloWorldServlet</servlet-name>
        <url-pattern>/hello</url-pattern>
    </servlet-mapping>

</web-app>
